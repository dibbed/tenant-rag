{
  "description": "Security Regression Suite of the Verification Pipeline (REQ-MTS-CI-002). The suite runs every test in 'paths'. The check fails when a test fails or is skipped, and when the collected tests differ from 'tests'. After adding, renaming or removing a security test, run the update command and commit this file with the test change, so the removal or addition is visible in review and in the Verification Summary.",
  "update_command": "python scripts/verification/run_suite.py security --update-manifest",
  "paths": [
    "tests/security",
    "tests/unit/test_edge_client_address.py",
    "tests/unit/test_edge_cors_policy.py",
    "tests/unit/test_edge_rate_limit_store.py",
    "tests/integration/test_edge_rate_limit_redis.py",
    "tests/integration/test_edge_uvicorn_server.py",
    "tests/integration/test_multi_tenant_isolation.py"
  ],
  "tests": []
}
